name: cleanforge
description: Generates a Flutter project with clean architecture, supporting selectable state management (GetX for now), including data, domain, presentation layers, and packages like dio, logger, dartz.


# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

vars:
  mode:
    type: enum
    description: Choose operation mode
    values:
      - new_project
      - existing_project
    default: new_project
    prompt: "Do you want to create a new project or use an existing one?"

  project_name:
    type: string
    description: Project name (required for new project, uses current directory for existing)
    default: ""
    prompt: "Enter your project name (press Enter to use current directory for existing project):"

  package_name:
    type: string
    description: Package name for imports (auto-detected)
    default: ""
    prompt: "Press Enter or give same name as project_name"


  state_management:
    type: enum
    description: Choose state management
    values:
      - getx
    default: getx
    prompt: "Choose state management:"